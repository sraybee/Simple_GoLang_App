apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My workflow
on:
  push:
    branches:
      - "**"
  workflow_dispatch:
jobs:
  build:
    steps:
      - name: Say hello from sraybyee
        uses: docker://golang:1.20.3-alpine3.17
        shell: sh
        run: |
          echo "Say hello from sraybyee"
